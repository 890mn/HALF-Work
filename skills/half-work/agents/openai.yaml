interface:
  display_name: "HALF-Work"
  short_description: "Proportionate task contracts and evidence gates"
  default_prompt: "Use $half-work to select the appropriate track, infer a minimal contract, pause only for material decisions, and execute changes only when my request or contract authorizes them."
